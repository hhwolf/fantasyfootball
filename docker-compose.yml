services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ffdash
    ports:
      - "5432:5432"
    volumes:
      - ffdash_pg:/var/lib/postgresql/data
volumes:
  ffdash_pg:
